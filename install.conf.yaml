- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.bashrc: .bashrc
    ~/.gitconfig: .gitconfig
    ~/.copilot/skills:
      create: true
      path: .copilot/skills

- shell:
  - [git submodule update --init --recursive, Installing submodules]
